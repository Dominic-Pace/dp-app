import React from 'react';

import { Container } from './styles';

const ProjectBlock = ({ imageUrl }) => {
  return (
    <Container imageUrl={imageUrl}>

    </Container>
  );
};

export default ProjectBlock;
