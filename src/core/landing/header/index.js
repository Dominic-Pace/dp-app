import React from 'react';

import { BackgroundImage, BackgroundOverlay, Container } from './styles';
const Header = () => {
  return (
    <Container>
      <BackgroundImage />
      <BackgroundOverlay />
    </Container>
  )
};

export default Header;
